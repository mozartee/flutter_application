import 'package:flutter/material.dart';

class TaksGoalPage extends StatelessWidget {
  const TaksGoalPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}